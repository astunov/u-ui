extends ./_layout.jade
block vars
  - var array = {body_class:'t-dev', layout_mod:'', title:'UI KIT | Goods'}
block content
  .u-container
    //- +
    +card()
      +cardHeader({"title": "TODO: equal height in a row"})
      +cardBody()
        +cardGoods()
          +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
          +goods({"classList": ['col-2']})
          .col.col-6
    //- ./
    //- //- +
    //- +card()
    //-   +cardHeader({"title": "Популярные категории"})

    //- //- ./

    article.block(style="max-width:970px;")

      //- +
      +card()
        +cardHeader({"title": "CONTAINER GRID"})
      //- ./

      //- +
      +card()
        +cardHeader({"title": "Goods * 2 row"})
        +cardBody()
          +cardGoods()
            +loop_while(3)
              +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
          +cardGoods()
            +loop_while(3)
              +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "1\\2"})
        +cardBody()
          +cardGoods()
            +loop_while(2)
              +goods({"classList": ['col-6', 'c-goods--wide'], "desc": true})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "1\\3"})
        +cardBody()
          +cardGoods()
            +loop_while(3)
              +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "1\\5"})
        +cardBody()
          +cardGoods()
            +loop_while(5)
              +goods({"classList": ['col-1_5']})
      //- ./

      //- +
      +card()
        +cardHeader({"title": "thumb 1\\8"})
        +cardBody()
          +cardGoods()
            +loop_while(8)
              +goods({"classList": ['col-1_8'], "thumbOnly": true})
      //- ./

    article.block
      //- +
      +card()
        +cardHeader({"title": "FLUID GRID"})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "Goods * 2 row"})
        +cardBody()
          +cardGoods()
            +loop_while(3)
              +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
          +cardGoods()
            +loop_while(3)
              +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "1\\3"})
        +cardBody()
          +cardGoods()
            +loop_while(3)
              +goods({"classList": ['col-4', 'c-goods--wide'], "desc": true})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "1\\5"})
        +cardBody()
          +cardGoods()
            +loop_while(5)
              +goods({"classList": ['col-1_5']})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "1\\6"})
        +cardBody()
          +cardGoods()
            +loop_while(6)
              +goods({"classList": ['col-2']})
      //- ./
      //- +
      +card()
        +cardHeader({"title": "thumb 1\\8"})
        +cardBody()
          +cardGoods()
            +loop_while(8)
              +goods({"classList": ['col-1_8'], "thumbOnly": true})
      //- ./


